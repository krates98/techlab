<%- include("../partials/header") %>
<%- include("../partials/adminnav") %>

</div><div class="container">
    <div class="col-sm">
        <table class="table table-dark">
            <th>
                Username
            </th>
            <th>
                Salary Till Date
            </th>
            <%  for(var i=1;i<=daysd;i++){ %>    
                <%  if(i<10){ xax = "0"+i.toString(); } else{ %>
                        <%     xax = i.toString(); } %>
                        <%   xax = xax + "/"+ curm +"/2020"; %>
                <% arr.push(xax); %>
                <% } %>
        <% for(var i=1;i<worker.length;i++){ %>
            <%    dsal = (worker[i].salary / daysm); %>
            
            <tr>
                <td>
            <%= worker[i].username %>
                </td>
                
                    <% for(var j=0;j<arr.length;j++){ %>
                        <% for(var k=0;k<dates.length;k++){ %>
                                
                    <% if(arr[j] === dates[k].date && worker[i].username === dates[k].username){ %>
                    <%   cx = cx + dsal %>
                    <%   k = dates.length; %>
                    <% }  %>  
                    <% } %>
                <%  } %>
                <%  if(cx > 0){ %><td class="bg-info"><%= Math.round(cx) %></td> <%} else { %>
                    <td class="bg-danger">0</td>        
                </td>
                
                <%  } %>
            </tr>
            <% cx = 0;%>
            <% dsal = 0; %> 
            <% } %>
    </table>
    </div>
</div>
</div> 
    
    
 <%- include("../partials/footer") %>
    

<%- include("../partials/footer") %>
    
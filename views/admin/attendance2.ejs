<%- include("../partials/header") %>
<%- include("../partials/adminnav") %>
   
</div><div class="container">
    <div class="col-sm">
        <table class="table table-dark">
            <th>
                Username
            </th>
            <%  for(var i=1;i<=daysd;i++){ %>    
                <%  if(i<10){ xax = "0"+i.toString(); } else{ %>
                        <%     xax = i.toString(); } %>
                        <%   xax = xax + "/"+ lasm +"/2020"; %>
                        <th><%= xax.substring(0, 2) %></th>
                <% arr.push(xax); %>
                <% } %>
        <% for(var i=1;i<worker.length;i++){ %>
            <tr>
                <td>
            <%= worker[i].username %>
                </td>
                
                    <% for(var j=0;j<arr.length;j++){ %>
                        <% for(var k=0;k<dates.length;k++){ %>
                            <% cxe = dates[k].time.substring(8, 11).trim(); %>      
                    <% if(arr[j] === dates[k].date && worker[i].username === dates[k].username && cxe === "PM"){ %>
                    <%   cx++; %>
                    <% }  %>  
                    <% } %>
                <%  if(cx > 0){ %><td class="bg-primary">P</td> <%} else { %>
                    <td class="bg-danger">A</td>        
                </td>
                <%  } %>
                <% cx = 0;%> 
                <%  } %>
            </tr>
            
            <% } %>
    </table>
    </div>
</div>
</div> 
    
    
 <%- include("../partials/footer") %>
    